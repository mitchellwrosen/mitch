\(name : Text) ->

''
#!/bin/sh

set -e

SHAKEFILE=./.shake/shake

if [ -x "$SHAKEFILE" ]; then
  exec "$SHAKEFILE" "$@"
else
  mkdir -p .shake
  cabal new-build shakefile
  cp $(cabal-plan list-bin ${name}-shakefile) "$SHAKEFILE"
  exec "$SHAKEFILE" "$@"
fi
''
